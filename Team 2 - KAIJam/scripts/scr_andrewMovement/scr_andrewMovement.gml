// Put Functions Below
function funct_notUsed(){

}

/*hInput = keyboard_check(vk_right) - keyboard_check(vk_left);
vInput = keyboard_check(vk_down) - keyboard_check(vk_up);

// Temp due to spd not existing (Needed for Debugging)
spd = 0.5; // Original 0.5
spdFinal = 0;
spdMax = 5;
lastMoveX = 0;
lastMoveY = 0;
moveX = 0;
moveY = 0;

if(hInput != 0 or vInput != 0){
	
	if(lastMoveX != hInput){
		lastMoveX = hInput;
		spdFinal = 0;
	}
	
	if(lastMoveY != vInput){
		lastMoveY = vInput;
		spdFinal = 0;
	}
	
	if(spdFinal <= spdMax){
		spdFinal += spd;
	}
	
	
	//dir = point_direction(0,0,hInput,vInput);
	//moveX = lengthdir_x(spd, dir);
	//moveY = lengthdir_y(spd, dir);
	
}
else{
	
	if(spdFinal > 0){
		spdFinal -= spd;
	}
	
}

if(spdFinal < spd){
	
	spdFinal = 0;
	lastMoveX = 0;
	lastMoveY = 0;
	
}

moveX = spdFinal * lastMoveX;
moveY = spdFinal * lastMoveY;

x += moveX;
y += moveY;*/