/// @description ???

draw_set_font(fnt_mainFont);


// Masking Code
maskingSurface = -1;
maskingTimer = -600; 

savedScaling = 0;
savedY = 0

hideLayers = 367;


// When it gets to 0, it will reset to 0 so it continues and doesn't cause a memory leak.

